# 渲染检查记录 · t6 v2「渲染与生成」（审计账本式纸墨）

项目：收储用途扩围与平台机会 · 制度机制的识别与检验（正式稿）
渲染节点：Designer（智见点评 · 制度设计）· 2026-09-16
内容单源：`expert-teams/智见点评-制度设计/expert-tasks/t5/output/融合稿_收储用途扩围与平台机会_正式稿.md`（194 行 / 22,825 B）

> **版本说明**：本目录为 **v2（本轮设计系统）**，与上一轮 `artifacts/`（v1，深青墨+琥珀金）并存，**不覆盖**。
> 外链亦为新 slug，历史链接保持可访问（已实测 200）。

## 交付物清单（路径 + sha256）

| 交付物 | 路径 | 大小 | sha256 |
|---|---|---|---|
| HTML5 视觉稿 | `html/收储用途扩围与平台机会_正式稿.html` | 51,308 B | `0889f538222ec0f94a75ebbdbdbe6982204eb1f1793a658d78b5dc496ecfd3c5` |
| PDF（A4，12 页，含 1 页横向宽表页） | `pdf/收储用途扩围与平台机会_正式稿.pdf` | 1,471,937 B | `7e6addd8795c21721e74406ba8837e33b4bbfaaabbbeeacfbe089f86c7564ab6` |
| PPTX（可编辑，25 页，python-pptx 原生对象） | `ppt/收储用途扩围与平台机会_正式稿.pptx` | 125,377 B | `9c66bd9779f75b98356f0f4f4f1582c04e8fb544e4f6eb466c743b52a9988a18` |

**外网链接（免登录）**：<https://yy.meizu.life/render/智见点评-制度设计/shouchu-kuowei-v2.html>
- 实测 **HTTP 200**，与本地 HTML **sha256 逐字节一致**（diff 校验通过）
- v1 历史链接 <https://yy.meizu.life/render/智见点评-制度设计/shouchu-kuowei.html> 仍为 200，**未被覆盖**

**可复渲染工程**：`build_report.py`（md → 账本 HTML）、`build_pptx.py`（python-pptx）、
`gates/run_gates_v2.py`（六门一键）、`shots/`（含 `pdf_pages/p01–p12.png` 逐页栅格化）。

## 设计系统（审计账本式纸墨 · 发丝线账册对开）

| 角色 | 值 | 用途 |
|---|---|---|
| 暖纸 | `#F1EEE7` | 页面底（**非纯白**，打印态亦用此色，去 AI-slop 纯白） |
| 近白卡 | `#FDFCF9` | 图卡、数字条、表格底 |
| 孔雀青 | `#10605A` | 主色：章头、表头、关键数字、对开主侧 |
| 古金 | `#9A6B22`（文本用 `#7E5618`） | 强调：图号 Ex.N、章头双线、金句引块 |
| 发丝线 | `rgba(26,23,20,.15/.08)` | 账册分隔：数字条分格、图卡边、表行、对开中缝 |

- **账册语汇**：`.chapter-head`（古金 3px 双线 + 章号 mono）、`.exhibit`（`Ex.N` 图号 + 衬线图题 + 口径副题）、
  `.numstrip`（发丝线分格 + mono `tabular-nums`）、`.frame`（**发丝线对开**：左可承接／右不可承接）、`.colophon`。
- **tabular-nums**：全局 `font-variant-numeric:tabular-nums`，数字条与表内数字用等宽 `--font-mono` 对齐。
- **零 JS**：全篇 0 个 `<script>`（门禁实测），图表为 CSS/HTML 原生条块。

## 四级标注（版式呈现，全文一致）

源文本标注实测：**【研判推断】54 ｜【引用】11（含口径限定）｜【测算】3 ｜【待补】2 ｜【估算】0**。

- 呈现为**行内色块角标**，五种角色统一：`tag-ref`（孔雀青底）／`tag-calc`（古金底）／
  `tag-est`（古金底+虚线边）／`tag-jud`（中性灰底）／`tag-todo`（警示底）。
- 复合标注（如「引用，案例归纳非统一统计」）拆为 **标签 + 限定语小字**，口径限定不丢。
- **【估算】在本稿源文本中出现 0 次**；仍保留其版式定义并在图例中列出，以保证四级体系完整、跨稿一致
  （已在 PPT 第 2 页显式说明）。
- 图例（legend）单独成块，置于封面之后，全篇不复述。

## 表格处理（宽表分页 + 窄屏降级 + 表单不断裂）

1. **用途—现金流矩阵（9 列宽表）**：PDF 中**独占一页横向页**（实测 p7 = 842×595 LANDSCAPE），
   其余页保持纵向；窄屏（≤760px）降级为**卡行**：表头隐藏，逐格以 `data-label` 前缀标注字段名，**不横向溢出**。
2. **表单页完整不跨页**：`.exhibit/.frame/.tblwrap tr/.numstrip/.legend` 全部 `break-inside:avoid`；
   实测 **8/8 表格的首行单元格与末行单元格同页**，无一断裂（评分卡、退出谱系、判别阈值、观察指标、全周期五段、三情景均完整）。
3. **英文/长串**：打印态 `table-layout:fixed` + `overflow-wrap:anywhere`，避免 A4 右侧裁切。

## 门禁结果（`gates/run_gates_v2.py` exit 0，全部通过）

| 门 | 检查项 | 结果 |
|---|---|---|
| G1 文字 | 禁例 token（含【融合说明】、专家实名、过程痕迹） | **0 命中**；品牌行在位；四级色块在位 |
| G2 数字 | 9 个锚点逐项 | HTML 全在 |
| G3 设计 | finesse detect | **P0 = 0** |
| | 对比度 AA ≥4.5:1 | **21 组全过**（paper+card+tag+封底） |
| | **320 / 375 / 414 / 768 / 1280 视口** | **全部 overflow = 0，无越界元素** |
| | 零 JS | script 计数 0 |
| G4 PPTX | 全页 speaker notes | **25/25** |
| | 原生对象 | **13 个原生 pptx 表格** + 259 形状，全部可编辑 |
| | 无越界形状 | 0 |
| | 数字锚点 / 「研判推断」 | 全在 / 36 处 |
| G5 PDF | 正文页脚逐页（含页码） | **10/10**；封面仅品牌行；封底无页脚 |
| | 宽表独占横向页 | landscape 页 = [7] |
| | 无右边距裁切（按页宽判定） | 越界 span = **0** |
| | 图卡/表单不跨页断裂 | **8/8 同页** |
| | **逐页栅格化留证** | **12/12 页** → `shots/pdf_pages/p01–p12.png` |
| | 数字锚点 | 全在 |

### 两个软项（accepted，有据）
- **P1 `side-stripe` ×1**：`.kicker-quote` 的古金 3px 左边框 —— 即「金句引块」，系 `report-template-fivepiece-v1.md` §4
  明文规定的智见研报组件语汇（pull-quote 装置，非卡片装饰）。
- **P2 `default-palette-token` ×3**：`--paper` 等暖纸令牌命中检测器的「手作暖色默认盘」启发式 ——
  但本轮配色是**队长明确指定**（暖纸 #F1EEE7 + 孔雀青 #10605A + 古金 #9A6B22），非模型自选默认，故接受。

## 与源文本的一致性核验（防静默丢内容）

- 正文段落：**38/38 命中**（探针报出的 2 处「未命中」经复核为探针瑕疵：md 侧去掉了标注、HTML 侧标注成为
  行内标签文本，可见阅读序一致）。
- 表格单元格：**120/120 命中**。
- **【融合说明】段**：源文本中**本不存在**该节（`grep` 0 命中），故「删去文末【融合说明】段」无对象可删；
  管线仍内置该剥离逻辑（`build_report.py` 报告 `fusion-section stripped: False`），以备后续版本出现该节时自动剔除。

## 交付回传（供队长验收门复核）

- 关键内容标记计数：HTML `tag-jud`54 / `tag-ref`11 / `tag-calc`4（含图例1）/ `tag-todo`3（含图例1）/ `tag-est`1（图例）；
  PPTX「研判推断」36 处、「待补」14 处、「测算」12 处。
- sha256：见上表，并落盘 `sha256sums.txt`。
- 外链：HTTP 200（v2），历史 v1 链接未被覆盖（仍 200）。

## 待用户确认

1. 是否用本轮**审计账本式纸墨**作为该系列的统一版式（v1 深青墨+琥珀金仍在 `artifacts/`，可对比择优）。
2. 是否调整 PPT（25 页）增删页或改口径。
3. 是否转正式稿最终版。视频按指令**不做**。

## 口径免责（随稿）

数字四级标注：引用 / 测算 / 估算 / 研判推断 / 待补。50%／20%／5% 为公开案例归纳、**非统一统计口径**，
只有分子没有分母，不可加总为全国结构、不可用于推算全国规模；深圳大学 5.336 亿元为披露金额口径，
床位与单套折算待补；单床 35.6 万元、单位面积 0.24 万元／平方米为**测算**。行业研究，不构成投资建议。
