# 领域包管理中心与 Harness 客户端开发计划

> 状态：第一阶段 W0/W1 Goal 已完成；整体 Goal 未完成。保护 API、持久异步任务、四页签、手动更新及本地 runtime 接线已通过相关回归/浏览器验证；兼容声明源已修正生成漂移，全库 934/934 通过。本文不是双实例或完整功能验收通过声明。
> 业务目标按“开发者自助交付 → 平台审核发布 → 租户自主选版运行”重组，当前状态见 [PACK-CENTER-BUSINESS-OBJECTIVE.md](PACK-CENTER-BUSINESS-OBJECTIVE.md)。
> 日期：2026-09-24 更新。
> 架构依据：[PACK-CENTER-DESIGN.md](PACK-CENTER-DESIGN.md)。
> 完成判据：[PACK-CENTER-GOAL-ACCEPTANCE.md](PACK-CENTER-GOAL-ACCEPTANCE.md)。实际开发在独立工作树进行，生产部署另行处理。
> 当前阶段 Goal：[PACK-CENTER-PHASE1-GOAL.md](PACK-CENTER-PHASE1-GOAL.md)。第一阶段只收口 W0/W1 的真实提交、审核、签名发布与授权下载；租户安装/更新/回退属于下一阶段。
> 确定要求：独立管理域名、多部署点、插件接入、DSH 核心零改动；客户端必须支持远程目录、拉取和更新。
> 暂定范围：邀请制合作方、公开 Git 提交 V2 领域包、人工审核；尚未选定的实际域名及服务端点采用配置值。

## 1. 首版完成标准

一个开发者能够在中心网页提交包，管理员能够审查固定内容并发布；两个独立 Harness 部署点能够浏览各自有权访问的包，选择版本下载、启用、检查更新、升级及离线回退。全流程有真实 API、权限和持久化支撑。

必须交付三组界面：

| 入口 | 首版页面 |
|---|---|
| 中心开发者端 | 我的包、提交版本、校验报告、提交详情与审核记录 |
| 中心管理端 | 待审列表、审核详情、发布目录、组织成员、部署点与凭据管理 |
| 设置 → 领域包 | **领域包目录 / 已安装 / 更新 / 来源设置** 四个页签；既有只读本地健康预览命名为“领域包校验” |

业务链：提交 → 固定快照与校验 → 送审 → 通过并发布 → 部署点下载 → 本地启用 → 检查更新 → 更新或回退。

首版不包含：ZIP 输入、私有 Git 凭据、任意可执行 DSH 插件、开放注册、中心推送安装、定时检查、静默自动升级、开发者 CLI 在线提交。现有离线 check 保留；这些后续项不影响网页闭环验收。

## 2. 阶段、依赖和演示目标

| 阶段 | 主要交付 | 前置 | 必须能演示的结果 |
|---|---|---|---|
| P0 协议与本地基础 | 接口契约、样例、可靠安装、显式启用与回退 | 无 | 将固定测试产物安装到隔离实例，启用、停用、离线回退；失败不丢旧版 |
| P1 中心提交 | 独立服务、身份组织、持久任务、开发者页面 | P0 契约冻结；可与本地安装并行 | 开发者登录、提交 Git、得到持久校验报告、送审 |
| P2 审核发布 | 管理页面、审核状态机、签名发布、授权分发 | P1 提交可用 | 审核员退回或发布同一快照，授权部署点可下载 |
| P3 客户端接入与更新 | 绑定中心、四页签、真实下载、版本检查、更新回退 | P0 安装 + P2 发布；UI 可按契约先行 | A/B 两点使用不同版本；发现更新、下载、确认启用、回退 |
| P4 联调与试点 | 权限/故障测试、迁移、运维与开发者文档、试点发布 | P0–P3 | 完整业务演示及中心断网、并发、失败恢复场景通过 |

关键依赖：协议 → 提交快照 → 审核发布 → 真实远程安装与更新 → 多实例验收。本地安装器、中心基础和界面原型在协议固定后可以并行；集成时不以 mock 结果代替真实链路。

里程碑顺序不等于把整个阶段做完才开始下一个。先用一个小 V2 样例完成最小贯通，再扩展页面与异常处理；每阶段的出口全部满足后才标为完成。

## 3. 可直接拆成开发任务的清单

### P0：契约与本地基础

| 编号 | 任务及落点 | 验收 |
|---|---|---|
| F01 | 在隔离开发环境建立基线；记录源码版本、依赖版本、构建/测试结果；核对设计列出的旧实现差距 | 基线可复现；不将 HANDOFF 的历史 682 项结果当本轮证据；不整理生产混合依赖树 |
| F02 | 新增 `packages/pack-contract/`：Submission/Release/Report/Operation schema、状态、错误码、权限范围、兼容性和版本规则 | 给中心与插件提供同一契约；未知协议/摘要算法明确拒绝 |
| F03 | 增加纯本地校验导出，复用 loader/validator；固定摘要与签名字节；新增两个有效版本及失败样例 | 中心/插件/离线工具对同一产物得到同一摘要及诊断；无 DSH 运行时依赖 |
| L01 | 新增 `src/host/pack-store.ts`、`pack-center-state.ts` 等拟定模块：不可变库存、唯一 staging、跨进程锁、幂等 operation、generation、有效前态 | 中断/重试/并发不丢旧版；坏状态阻止写入并可从验证快照恢复 |
| L02 | 本地启用映射接入 `runtime-pack.ts` 与发现逻辑；缓存纳入 generation；在途任务绑定完整包及依赖快照 | 安装不自动生效；全部中心包可停用；旧任务的惰性资源读取也保持旧版；没有可靠引用跟踪前不启用自动 GC |
| L03 | 旧 vendor 包接管及离线回退：保留来源、校验备份、按路径排除旧发现、可恢复本地管理 | 同 ID 不重复参与运行；不误屏蔽 builtin/workspace；不伪造中心审核身份 |

F02 冻结后，L01–L03 与 P1 可分开推进。L02 修改属于专家库插件内部代码，不修改 Harness 核心 loader。

### P1：中心基础与开发者提交

| 编号 | 任务及落点 | 验收 |
|---|---|---|
| C01 | 新增 `apps/pack-center/` 独立 Web/API/Worker 构建与开发启动；数据库迁移、任务表、制品存储适配器 | 中心无需启动 DSH；不进入插件原有默认构建/发布文件 |
| C02 | OIDC 登录、邀请、组织成员、开发者/审核员/管理员权限、审计；首位管理员初始化 | A/B 两组织隔离；禁用人员失去写权限；不能伪造 orgId 越权 |
| C03 | Git 抓取任务：固定 commit、规范化、校验、生成差异/归档、保存报告；任务租约与重试 | API 返回 jobId；Worker 重启可恢复；送审内容固定；不执行仓库代码 |
| C04 | 提交 API 与状态机：草稿、校验、送审、撤回、修订；所有权与幂等校验 | 待审快照不可覆盖；修订保留原记录；跨组织读取被拒 |
| U01 | 中心开发者 UI：我的包、三步提交、报告、详情时间线 | 用户能从页面完成提交；错误定位清楚；刷新页面后状态仍在 |

开发者样例直接提供合法 V2 包，不把旧样例适配问题交给首个试用者。包内说明必须写清支持的来源和结构。

### P2：审核、发布与分发

| 编号 | 任务及落点 | 验收 |
|---|---|---|
| C05 | 审核 API：按快照和 expectedVersion 批准/退回/拒绝；审核人与提交人分离 | 两人同时审核只允许一次状态迁移；作者不能自批 |
| C06 | 发布任务：复用已审归档，生成签名清单，唯一版本约束、失败重试、下架、可见范围变更审核 | 分支移动不影响发布字节；同版不可覆盖；失败重试不重新审核新内容 |
| C07 | 授权目录、版本清单、下载许可；绑定码兑换、站点凭据轮换与撤销 | 凭据只访问授权包；机器身份不能审核；无权时不泄露私有元数据 |
| U02 | 管理 UI：待审列表、内容/差异/报告/时间线、通过并发布、退回意见 | 完整完成退回→修订→通过；发布失败与审核失败分开展示 |
| U03 | 发布目录、组织成员、部署点管理页面 | 可邀请成员、登记实例、绑定/撤销凭据、下架版本并保留审计 |

到此必须形成第一个真实审核发布产物，而不是先把全部管理页面做完。发布签名密钥不提供给 Git 抓取 Worker。

### P3：Harness 远程目录、拉取与更新

| 编号 | 任务及落点 | 验收 |
|---|---|---|
| L04 | 新增 `src/host/pack-center-client.ts`、`pack-center-routes.ts`：中心认证、连接配置、下载许可、签名验证、异步安装 operation | 管理员浏览器只调本地受保护 API；机器密钥不返回前端；下载失败保留旧版 |
| L05 | 更新检查：查询授权版本、版本排序、兼容性及依赖预检、当前/缓存/候选版本对照 | 有更新、不兼容、缺依赖、无权、网络失败准确区分；检查不会安装或激活 |
| L06 | 更新操作：固定 releaseId/digest、下载验证、安装；显式“更新并启用”；离线回退、停用和卸载；每次启用/回退按当前插件、schema 和协议能力复验兼容性 | 更新前保留旧版；失败旧版仍用；缓存包不沿用过期兼容结论；有活跃依赖/在途任务时阻止破坏性删除 |
| U04 | 在“设置 → 领域包”提供四页签版本管理 UI，经 `src/client/index.tsx` 的现有扩展点注册 | 远程目录、已安装、更新、来源设置全部调用真实接口；具备加载/空/失败/无权状态 |
| L07 | `Config`、`ToolsConfig`、`settings.ts` 同步；兼容旧管理卡及本地来源标识；纯本地加载链回归 | 不丢既有设置；中心不连通时其他领域包仍正常；no-network 检查通过 |

UI 可以在 P0 契约固定后使用夹具开发；P3 完成必须换成真实中心及真实本地安装器。远程管理允许联网，任务执行与包加载不临时联网取包。

### P4：集成、运维与交付

| 编号 | 任务 | 验收 |
|---|---|---|
| Q01 | 中心 + 两个隔离 DSH 实例完整链路与权限矩阵 | 不同身份和部署点的提交、审核、下载、安装边界均有证据 |
| Q02 | 并发、篡改、断网、进程中断、缓存切换、旧包迁移及回退 | 每个关键失败点都有可复现测试；旧版与审计记录保留 |
| O01 | 插件自带部署前检查；准备宿主重启接线，覆盖计划重启与 systemd 启动 | 测试环境缺 bundle 时在停旧进程前阻断；生产接线不改 DSH 核心 |
| O02 | 中心独立部署配置、DNS/TLS、身份服务、制品存储、签名密钥、备份恢复与监控说明 | 从备份恢复数据库和产物后，发布引用仍完整；密钥通过专用凭据机制管理 |
| D01 | V2 样例、开发者指南、审核员指南、插件安装/更新/回退指南、升级与回滚说明 | 新开发者按文档可完成提交；管理员按文档接入第二实例 |
| R01 | 隔离验收通过后进行一个部署点的小范围试点，再扩展 | 按试点反馈确认无回归后扩大；记录实际版本与回退点 |

O01 可提前并行，必须在涉及现有生产实例的重启前完成。重启前检查不是客户端每次拉包都要执行的步骤。

## 4. 设置 → 领域包的四个页签

| 页签 | 内容 | 操作与结果 |
|---|---|---|
| 领域包目录 | 名称、发布者、简介、授权版本、兼容性、依赖、当前已装情况 | 手动刷新；查看详情和变更；选择固定版本“下载并安装”，默认未启用 |
| 已安装 | 当前启用版、已下载其他版本、来源、完整性、历史记录 | 启用/停用、回退、卸载；对依赖和在途任务说明阻断原因 |
| 更新 | 当前启用版、最新可见版、推荐兼容版、变更说明、阻断原因、检查时间 | “检查更新”；逐包“下载更新”或显式“更新并启用”；失败保留旧版 |
| 来源设置 | 中心地址、部署点绑定、连接及授权结果 | 绑定、测试连接、轮换、解绑；首版同时绑定一个中心，替换中心不能自动改写既有包归属 |

更新规则在 F02 固定，L05 实现：

1. 新中心版本采用明确的 SemVer 规则；默认检查稳定版，不按字符串或发布时间排序。例如 `1.10.0 > 1.9.0`，build metadata 不提高版本优先级。旧的非规范版本只能显式迁移，不猜测大小。
2. 同时展示最新可见版与最新兼容候选；比当前版本低的兼容版本不自动当成更新。预发布版仅在明确选取时安装。
3. 检查版本针对同一个中心、拥有者和 packId；任何同版本不同摘要都视为异常，不能静默覆盖。
4. “当前已启用 1.0、已下载 1.1”应提示可启用 1.1，不重复下载；更新一个已停用包不会隐式启用它，除非用户选择了带启用的明确动作。
5. 检查更新只读取元数据，不修改启用状态、不拉取 Git、不执行安装；不绑定每次对话或每次任务触发。
6. 更新确认固定 releaseId 与摘要；真正下载前重新检查授权、下架状态；每次启用、更新并启用、离线回退均重新检查本地 generation、内容、依赖及当前插件/schema/协议兼容性。
7. 无权、中心离线、请求超时都不能显示“已是最新”；缓存数据必须显示最后检查时间和过期提示。
8. 默认逐包操作，不引入无法保障依赖一致性的“一键全部更新”。启用、停用、更新、回退、卸载都检查反向依赖：若 A 锁定 B 的旧 release，不能单独更新 B；给出受影响包及人工调整顺序，不自动级联切换。
9. 文件已下载但启用预检失败时显示“下载完成、未启用，旧版继续运行”，不能将“更新并启用”报告为成功。相同幂等键不得对应不同目标；过期 generation 返回冲突，不暗中改选另一版本。

拟定本地路由以 `/plugins/dsh-expert-library/manage/center` 为前缀：

| 路由 | 行为 |
|---|---|
| `GET /connection`、`POST /bind`、`POST /unbind` | 查看连接、绑定、解绑；只返回脱敏信息 |
| `GET /catalog`、`GET /releases/:id` | 经插件宿主查询远端授权目录与详情 |
| `POST /check-updates` | 执行显式更新检查，返回结果与 checkedAt；返回每包 current/latestVisible/candidate/blockedReasons |
| `GET /installations` | 读取本地库存及启用状态；不依赖中心在线 |
| `POST /operations`、`GET /operations/:id` | 创建/跟踪下载、安装、启用、更新、回退、停用、卸载操作；写请求有幂等键与预期 generation |

中心版本查询复用 `/api/v1/releases` 并按有权访问的 packId 分页筛选。首版不依赖中心向部署点推送消息，也不需要部署点新增公网入口。

## 5. 分工与并行安排

以下为建议职责分工，不假设已经有对应人员到岗：

| 工作流 | 负责范围 | 并行条件 |
|---|---|---|
| 协议与集成负责人 | F01–F03，接口变更管理，集成样例和最终验收 | 优先完成；给其他工作流提供固定 schema 和夹具 |
| 中心后端 | C01–C07，数据迁移、身份、Worker、发布分发 | F02 冻结后开始，优先交付提交和发布最短链路 |
| 插件端 | L01–L07，本地库存、启用、远程访问与更新 | 与中心后端并行；先通过固定签名测试产物验证 |
| 前端 | U01–U04，中心页面与 Harness 四页签 | 按契约先行，随后逐页接真实接口 |
| 测试与部署 | Q/O/D/R，隔离实例、恢复、指南、试点 | 早期准备夹具和验收环境；生产变更在验证后执行 |

多人/多 Agent 同时工作时按上述文件范围分配；共享 schema、package.json、src/index.ts 等由集成负责人统一合并。只读评审可以并行，不并发改同一接口定义。

若由单人或单主 Agent 执行，按 F → L01/L02/L03（依次满足依赖）→ C → U/L04–L07 → Q/O/D/R 推进，不因为分工表引入不必要的服务或组织层级。

## 6. 验证与发布门槛

| 测试层 | 必须验证的行为 |
|---|---|
| 契约与摘要 | 跨端一致性、不可变 release、未知协议拒绝、有效/篡改签名、确定性排序 |
| 中心状态与权限 | 跨组织读取拒绝、自审拒绝、并发审核、重试幂等、范围扩大审核、下架与下载授权 |
| Worker | 分支变动、源抓取失败、超限、任务崩溃恢复、送审快照不变化、无仓库代码执行 |
| 本地安装 | 唯一 staging、并发 generation、旧版本保全、坏状态恢复、完整性异常、离线回退 |
| 更新 | 版本比较、兼容候选、授权变化、依赖/反向依赖、已缓存候选、失联不是最新、检查不激活 |
| 运行与迁移 | 安装不启用、全部停用、缓存失效、在途任务保留、旧路径只排除一次、无同 ID 双加载 |
| 页面 | 三类用户全流程、加载/空/错误/无权状态、报告定位、按钮状态与后端权限一致 |
| 既有回归 | 插件 typecheck/build、现有相关测试、本地 loader no-network；集成里程碑执行完整插件测试 |

现有命令 `pnpm typecheck`、`pnpm test` 作为插件基线；后者已经包含 build。新增中心与契约包独立测试入口。日常改动跑受影响测试，阶段集成再跑完整回归，不对每个文档或 UI 文案改动重复全量构建。

试点必须演示：开发者提交 v1 → 审核退回 → 修订通过 → A/B 安装 v1 → 发布 v2 → A 检查并启用 v2，B 仍使用 v1 → 断开中心及 Git → A 回退 v1 → 恢复网络并下架 v2 → 新下载被拒。过程中核对每个 release、摘要、授权和本地 generation。

变更发布分开处理：中心可独立部署；插件代码升级按宿主现有流程构建安装及受控重启；日常领域包安装/更新不应要求重启 DSH。插件升级回滚还须验证本地状态格式向后兼容，不能只保留旧 npm 包而忽略新 state 版本。

## 7. 启动开发所需条件与范围假设

可以立即开发且不依赖正式域名：契约、同源校验入口、样例、库存与启用机制、中心本地环境、页面、隔离端到端测试。

| 事项 | 目前处理方式 | 最晚确定时间 |
|---|---|---|
| 邀请制 / 开放注册 | 计划按邀请制；改为开放注册需补注册治理工作量 | P1 身份流程定稿前 |
| Git / ZIP / 可执行插件 | 计划按 Git 领域包；另外两类不混入现有任务 | 对应输入和运行范围开发前 |
| 正式中心域名 | 全程配置；开发使用隔离测试地址 | P4 部署前 |
| OIDC 身份服务 | 开发使用隔离测试身份服务，生产服务未选定 | 生产登录联调前 |
| 数据库、制品存储、签名凭据位置 | 按独立中心配置，不读取 DSH 的生产凭据 | P4 部署前 |
| 试点组织、审核员、两个测试部署点 | 使用测试身份/独立目录，禁止生产故障注入 | Q01 联调前 |

工期按任务估算，不把人日当日历天。本轮不编造完成日期；F01–F03 结束后，结合身份服务接入、现有运行版本兼容性和可用开发人力，逐项给出范围估算并形成排期。里程碑按出口验收，不按日期自动认定完成。

## 8. 第一批执行顺序与交付记录

第一批 F01、F02、F03 已形成基线、协议及真实样例。随后推进 L01–L03 本地基础组件与 C01 中心持久层；本批隔离构建通过，插件 786 项及协议/归档 85 项全部通过。实际 provider、远端链路、页面及完整任务生命周期尚未接通，不因此宣布 P0–P4 完成。

2026-09-19 后续：隔离树 `/root/zhijian/dsh-pack-center-dev.ZGtty5` 已实现中心身份/提交/审核 HTTP API、Git 固定快照 Worker、签名发布及授权失败重试。真实 API 集成证明移动 Git 分支仍发布原批准字节，重复投递不改写。具体源码、冻结回归、运行入口与剩余项见该树 `PACK-CENTER-P1-EVIDENCE.md` 及 `apps/pack-center/README.md`。原插件运行代码、DSH 核心及生产服务未变。

2026-09-19 本批：隔离树已实现 C07 部署点登记/一次性绑定、凭据轮换/撤销和授权目录下载，补齐 C06 下架与独立分发范围审核。真实 Git→批准快照→机器 HTTP 下载贯通，两机器身份权限隔离及旧许可撤权均通过；1042 项回归全部通过。证据见 `/root/zhijian/dsh-pack-center-dev.ZGtty5/PACK-CENTER-P2-EVIDENCE.md`；不是两个真实 DSH 实例验收。

2026-09-19 网页批次：隔离树已接通 U01–U03 主要页面，真实 Chromium/OIDC/PostgreSQL/HTTPS Git/Worker 跑通提交→退回→独立修订→批准→签名发布，以及组织邀请和两部署点绑定码管理；增加会话恢复、传输失败、保存取消与纯文本安全验证。采用原生同源 ES modules；入口和证据见 `/root/zhijian/dsh-pack-center-dev.ZGtty5/apps/pack-center/README-web.md` 与 `PACK-CENTER-P3-WEB-EVIDENCE.md`。原插件运行代码、DSH 核心及生产服务未变。

2026-09-20 宿主客户端批次（L04-A）：独立 pin、一次性绑定、私密凭据 CAS、授权目录/详情、短期 grant 和真实签名下载已接本地 pack-store。默认仅缓存；解绑保留 pins，已提交安装支持离线精确回放；connection→inventory 锁序协调解绑与提交。真实中心到两宿主客户端目录已贯通，不等于两套 DSH。代码、接口说明 `PACK-CENTER-HOST-CLIENT.md` 和冻结证据 `PACK-CENTER-P4-HOST-CLIENT-EVIDENCE.md` 均在隔离工作树 `/root/zhijian/dsh-pack-center-dev.ZGtty5/`。

2026-09-20 本批（L04-B/L05–L07/U04）：受保护本地接口、白名单 DTO、持久异步 operation/进度/恢复、实际插件配置和三类运行基底 preflight 已接通；四页签支持绑定、目录、安装/启停/回滚/卸载和手动更新。最后编译版本相关回归 162 项通过，中心 176 项和协议/归档 85 项通过，两个 compiled 浏览器流程通过并留存 24 张截图。随后按确认增加独立 V2 pipeline 兼容声明源，生成器 `--check` clean，受影响 44 项及插件全库 934/934 通过；原始 928/932 失败 TAP 仍保留作问题定位证据。两套真实 DSH 已完成进程/接口隔离探针，但不是实际版本切换或断网验收。证据见 `/root/zhijian/dsh-pack-center-dev.ZGtty5/PACK-CENTER-P5-HOST-UI-EVIDENCE.md`，不是完整实际 DSH 部署验收。

下一批按 §9 先固定合规公开包和隔离环境，完成真实发布证据，再验收两个实际 DSH 的 v1/v2 安装、显式启用、离线回退和长任务生命周期；并行准备 U03 治理边缘路径及故障注入工具。S3、硬配额、恢复演练与第二实例复建仍未完成，不将组件链路折算为 Goal 完成。

每个任务完成应附：代码/文档位置、实际验证结果、残留限制、可演示入口。跟踪时使用本计划的稳定任务编号；实际状态见目标与验收文档的执行记录，不因已有相似代码而自动标完成。

本开发计划取代泛化的“做管理中心”事项，但不覆盖或修改 HANDOFF 的历史状态；新的进展单独记录。Goal 已进入隔离开发与测试阶段，生产部署不在本轮范围。

## 9. 下一轮拆分：从组件完成走到业务闭环

现有 P0–P4 适合描述能力域，但 P4 过大，不能直接驱动执行。下一轮改按业务闭环拆成六个波次（W0–W5）。每个波次同时维护两种状态：**实现状态**（代码是否具备能力）和**验收状态**（真实环境是否留下证据）。只有波次出口通过，才允许进入依赖它的下一波；单元测试、编译浏览器测试和模拟中心不能替代真实出口。

### W0：锁定真实样例和验收环境

| 任务 | 内容 | 前置 | 出口证据 | 对应验收 |
|---|---|---|---|---|
| D0.1 合规公开包基线 | 为本轮选定一个真实公开 Git V2 包；提交字段、`pack.json` 和发布清单的包身份按当前契约一致，新包优先使用 `orgslug.packslug`，实体 ID 与引用按契约保持一致。安全的历史裸 ID（如 `macro-capital-analyst`）可在全局唯一、组织归属和清单一致时使用。 | 无 | `dsh-pack check` 通过；版本/ID/实体引用对照表 | A01、A02 |
| D0.2 双版本样例 | 同一 `packId` 准备 v1/v2，差异可在运行结果中观察；两版均有固定摘要。 | D0.1 | 两份包的 `pack.json`、tree digest、artifact digest | A05、A15 |
| D0.3 隔离环境清单 | 固定中心 DB/schema、制品根、签名公钥、OIDC 测试身份、两个 DSH_HOME/profile/workspace；写明端口、版本和清理方式，不读取生产凭据。可与样例准备并行。 | 无 | `environment.json`、源码指纹、启动前检查日志 | A07、A22、A24 |

**波次出口 W0-Gate：**样例包可校验、两版本可区分、两个租户目录和中心环境可独立启动。若尚无可复现公开样例证据，允许用隔离 Git fixture 做 W1–W3 的技术演练，但公开 Git 业务验收保持待办；演练不能替代 W1-Gate，也不能写成开发者自助发布完成。

### W1：开发者自助发布最小闭环

| 任务 | 内容 | 前置 | 出口证据 | 对应验收 |
|---|---|---|---|---|
| D1.1 提交 v1 | 以开发者身份从网页创建草稿、触发异步校验，记录 submission/job/commit/report。 | W0-Gate | API 与页面记录；刷新后状态一致 | A01 |
| D1.2 负向校验 | 提交错误结构、错误 ref、抓取失败或超限输入；确认无脚本执行、无可发布快照。 | W0-Gate | 失败码、任务日志、无 release 证明 | A02 |
| D1.3 退回与独立修订 | 审核员退回 v1，开发者创建新 submission 修订；旧快照、意见和新快照均保留。 | D1.1 | 两 submission、两 snapshot、审核时间线 | A03 |
| D1.4 通过并发布 | 送审后移动源分支/tag，再由独立审核员批准；验证两人同时批准只有一次有效决定，发布失败重试仍只消费批准归档、同一版本只有一个固定产物。记录 release、source commit、三个摘要和签名公钥标识。 | D1.3 | 原快照与下载摘要对照、并发决定记录、重试后的唯一 release 和签名清单 | A05、A06 |

**波次出口 W1-Gate：**至少一个符合当前包身份契约的真实公开 Git 包完成“提交 → 校验 → 退回 → 修订 → 批准 → 签名发布”，并能由授权机器下载；发布物与批准快照摘要一致。没有这个出口，不进入双租户版本演示。

### W2：两个真实租户的安装、显式启用和版本分叉

所有租户操作均从 **设置 → 领域包** 的四个页签进入。当前租户隔离按独立 DSH 部署点建模；同一 DSH 内多用户各自持有独立版本空间尚未实现，不列作本轮已交付能力。

| 任务 | 内容 | 前置 | 出口证据 | 对应验收 |
|---|---|---|---|---|
| T2.1 启动两个 DSH | 用独立 `DSH_HOME`、profile、workspace、pack-center 根和端口启动 A/B；确认插件来自当前隔离树，DSH 核心和生产服务没有改动。 | W0-Gate | 进程、profile、端口、插件版本和状态目录清单 | A23、A24 |
| T2.2 独立绑定 | A/B 分别兑换一次性绑定码，核对 centerId、deploymentId、公钥指纹、凭据有效期；前端和日志不出现机器密钥。 | W1-Gate、T2.1 | 两份脱敏连接视图、撤销矩阵 | A04、A07 |
| T2.3 安装与启停 v1 | A/B 从真实中心下载 v1，验证签名/归档/tree；默认未启用，再分别显式启用并查询 runtime。覆盖单包停用、全部中心包停用，再次启用；builtin/workspace 保持既有语义。 | T2.2 | 两点 state、operation、generation、启停前后 runtime 输出 | A08、A09、A10 |
| T2.4 检查更新与版本分叉 | 覆盖 1.9/1.10、稳定/预发布/不兼容候选；检查只读元数据。A 先缓存 v2，再“更新并启用”且不重复下载，B 继续 v1；中心再次发布不触发任一点自动切换。 | T2.3、D0.2 | 当前/可见/兼容候选对照、检查前后 state、下载次数、A/B runtime 与页面记录 | A11、A13、A15、A21 |
| T2.5 正反向依赖约束 | 包 A 固定依赖包 B 的旧版；尝试更新、回退、停用及卸载 B。阻断不兼容操作，列出受影响包，不自动级联；修正依赖选择后才允许提交。 | T2.3 | 固定依赖清单、阻断原因、前后 state/runtime、成功恢复操作 | A14 |

**波次出口 W2-Gate：**真实 DSH A/B 均完成 v1 安装，A 使用 v2、B 仍使用 v1；全停用和正反向依赖阻断有效，所有版本和摘要可从本地状态与 runtime 复核。编译浏览器流程只能作为辅助证据，不能代替本出口。

### W3：断网、失败和长任务安全

| 任务 | 内容 | 前置 | 出口证据 | 对应验收 |
|---|---|---|---|---|
| R3.1 中心/Git 断开 | 停中心并阻断 Git；A/B 执行已启用包，A 从缓存回退 v1 并按当前能力/依赖复验。更新页有缓存时标明过期及最后成功时间，无历史时明确未获取；不误报最新或空目录。 | W2-Gate | 网络调用日志、运行结果、回退前后 state、过期/未获取页面 | A12、A18、A21 |
| R3.2 下载/启用失败 | 注入篡改归档/清单/库存、未知协议、启用预检失败和 stale generation；确认旧启用版继续运行，失败操作可重试且不重复提交。 | W2-Gate | 前后摘要、operation 回执、旧版本仍 active | A09、A13、A16 |
| R3.3 长任务快照 | 任务运行中切换 A 的版本；新任务使用新快照，旧任务及惰性资源继续引用旧目录，旧目录不能提前删除。 | W2-Gate | 任务 ID、快照/generation、目录引用和完成结果 | A17 |
| R3.4 state 损坏恢复 | 损坏 state、在提交前/后终止进程；验证只读恢复或明确不可用，不把损坏状态当空库。 | W2-Gate | 损坏文件、恢复模式、旧 state 和审计 | A16、A19 |

**波次出口 W3-Gate：**中心不可达时已有包仍能运行和回退；任何下载、校验、启用失败都不丢旧版；长任务和状态恢复有真实日志。没有 W3-Gate，不能声称离线运行或故障恢复完成。

### W4：治理边缘、迁移和可复现部署

| 任务 | 内容 | 前置 | 出口证据 | 对应验收 |
|---|---|---|---|---|
| G4.1 权限矩阵收口 | 逐项跑自审、跨组织、禁用成员、机器凭据访问审核接口、撤销 A 保留 B、下架和范围变更；更新检查后撤权/下架时，新下载重新授权；补齐管理网页边缘操作。 | W1-Gate、W2-Gate | 请求/响应矩阵、无泄露扫描、审计记录、失败页面 | A04、A07、A12、A21 |
| G4.2 legacy 接管 | 用真实旧 vendor 包完成接管、停用、恢复；确认同 ID 不双加载，legacy 不被伪标为中心 release。 | W2-Gate | 迁移前后发现列表、来源收据、恢复记录 | A20 |
| G4.3 中心/Worker/制品恢复 | 重启中心、终止 Worker、恢复 DB 与制品备份；确认任务可重领、审核引用和签名发布仍可下载。 | W1-Gate | 恢复前后摘要、job lease、release 下载记录 | A22 |
| G4.4 第二实例复建 | 按文档从空目录复建第二租户；执行缺 bundle/缺配置的启动前检查，确认不会先停旧进程。 | W0-Gate、W3-Gate | 重建命令、版本清单、检查失败和成功日志 | A24 |
| G4.5 部署存储与资源限额 | 在隔离环境补齐 S3 兼容制品适配、独立部署配置及 Worker 的磁盘/内存/时间硬限额；验证超限可终止回收，制品恢复后仍匹配签名摘要。 | W0-Gate | 存储适配验证、超限退出/回收日志、隔离部署与备份恢复说明 | A02、A22、A24；C01/C03/O02 |

**波次出口 W4-Gate：**权限、迁移、恢复、部署存储/硬限额和第二实例均有可复现证据；部署指南、配置、检查脚本与实际行为一致。

### W5：总验收与交付冻结

| 任务 | 内容 | 出口 |
|---|---|---|
| C5.1 验收矩阵收口 | 逐项更新 A01–A24，只使用当前源码、当前环境和对应运行证据；A21 串起开发者、平台、租户三类角色页面及“设置 → 领域包”四页签，覆盖提交到更新/回退与进度/空/失败/无权状态。没有证据的条目保持“待验收/阻塞”。 | `PACK-CENTER-GOAL-ACCEPTANCE.md` 无未解释的必需条目 |
| C5.2 全量回归与边界检查 | 插件构建/全库回归、中心/契约/归档回归、no-network、DSH 核心零修改、敏感信息扫描、源码指纹。 | 退出码、TAP、指纹和差异记录齐全 |
| C5.3 交付文档冻结 | 开发者、审核员、租户管理员、运维/恢复指南与隔离环境清单一致；明确生产试点尚未执行。 | 第二实例按指南可复建；Goal 才可评估完成 |

## 10. 并行规则和当前关键路径

可以并行：D0.3 环境准备与 D0.1/D0.2 样例准备；W0 的环境准备与 G4.2 legacy 代码补强；W1 的页面边缘治理与 T2.1 本地 DSH 启动脚本；W3 的故障注入工具与 W1 的真实发布准备。前置出口约束业务验收，工具与实现可提前准备。

不能越过的关键路径是：

```text
符合当前契约的真实包
  → W1-Gate：真实审核发布物
  → W2-Gate：两个真实 DSH 的 v1/v2 分叉
  → W3-Gate：断网回退与失败保旧
  → W4-Gate：恢复、迁移、第二实例
  → W5：A01–A24 总验收
```

当前优先执行 D0.1–D0.3，再交付 D1.1–D1.4 的可复现真实发布证据。`macro-capital-analyst` 已有历史发布记录，但缺少本轮可追溯运行索引，需重新核验；也可选用其他符合当前契约的公开 Git 包。隔离样例可用于技术联调，证据须标为“技术演练”。
