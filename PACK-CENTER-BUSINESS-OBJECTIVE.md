# 领域包中心：业务目标与实现状态

日期：2026-09-24

这份文档把领域包中心按业务责任重新组织。设计文档回答“系统应该是什么”，本文件回答“谁得到什么能力、怎样才算交付、当前证据到哪一步”。状态以当前隔离实现和验收记录为准，不把组件测试、页面截图或历史回归扩大解释为完整业务完成。

## 1. 业务闭环

领域包是一种可审查、可签名、可独立安装的版本化产品。完整闭环只有三段，缺一段都不能称为领域包交付完成：

```text
开发者自助交付  →  平台审核与发布  →  租户自主选择版本并运行
       ↑                  │                     │
       └── 新版本/修订 ────┘                     └── 检查更新、回退、离线继续运行
```

业务上要解决的是版本治理和租户自主权：开发者不再依赖平台工程师手工拷贝目录；平台仍掌握内容审核、签名和分发范围；租户决定何时下载、是否启用和何时回退。平台发布不会自动改变任何租户正在使用的版本。

## 2. 三类操作者及完成定义

### 2.1 开发者：自助发布和维护自己的领域包

开发者通过邀请登录进入所属组织，创建或维护一个全局唯一的包 ID，提交公开 Git 仓库 URL、固定 ref、版本说明和预期分发范围。中心异步固定 commit，生成不可变校验快照、结构报告、内容预览和与上一版本的差异。开发者可以看到失败原因、根据意见创建独立修订并再次送审；已经送审的快照不能被原地覆盖。

开发者完成的业务结果是：

- 能从网页完成“创建/提交 → 查看报告 → 送审 → 根据退回意见修订”；
- 每一次提交都有可追溯的 commit、内容摘要、归档摘要和状态；
- 不能绕过审核直接发布，也不能读取其他组织的私有提交；
- 在线 CLI 不是首版业务入口，现有离线 `dsh-pack check` 只承担提交前自检。

**当前状态：第一阶段通过，整体租户闭环未完成。** 真实公开 Git 仓库 `weixkcornell/macro-capital-analyst` 的 V2 `2.2.0 → 2.3.0` 已在真实 Chromium、OIDC、PostgreSQL、HTTPS Git 和 Worker 链路中完成提交、异步校验、退回、独立修订、送审；审核后的 `2.3.0` 已签名发布并由授权机器通过 HTTP 下载复验。当前契约允许满足全局唯一、组织归属和清单一致性的安全历史裸 ID，新的包仍推荐 `orgslug.packslug`。租户在“设置 → 领域包”的真实安装、启用、更新、回退和双 DSH 运行属于下一阶段。证据见 [PACK-CENTER-PHASE1-EVIDENCE.md](PACK-CENTER-PHASE1-EVIDENCE.md)。

### 2.2 平台：审核、签名发布和分发治理

平台管理员负责组织、成员、审核范围、部署点和凭据；审核员查看固定快照的实际内容、校验报告和差异，退回或批准；发布 Worker 只消费批准快照，生成不可覆盖的签名发布物。发布后平台可以下架或通过独立治理流程调整分发范围，但不得重写同一 `packId + version` 的内容。机器凭据只能读有权目录和下载，不能提交、审核或管理成员。

平台完成的业务结果是：

- 审核决定与发布任务分开留痕，批准内容和下载内容摘要一致；
- Git 分支或 tag 在审核后移动，不改变已批准字节；
- 并发批准、发布重试、撤权、下架和范围变化不会绕过权限或覆盖旧产物；
- 签名、授权、归档和树摘要由中心与租户本地再次验证。

**当前状态：第一阶段发布闭环通过，整体平台交付未完成。** 中心 API/Worker、签名发布、组织与部署点管理、机器目录/下载授权已用真实公开 Git 链路验收；第一阶段中心回归 70/70、协议与归档 85/85、真实 Chromium 1/1、真实 HTTP 1/1。治理页面完整边缘覆盖、硬配额、S3/生产部署、备份恢复、第二实例按指南重建仍没有完整验收证据。

### 2.3 租户：在自己的界面选择是否更新并控制运行版本

租户管理员只管理自己的部署点。版本管理入口统一为 **设置 → 领域包**，其中提供四个页签：领域包目录、已安装、更新、来源设置。既有只读本地健康预览命名为“领域包校验”。租户先绑定可信中心，再手动刷新授权目录；下载并验证后版本只进入本地库存，默认不启用。租户明确点击启用、停用、更新并启用、回退或卸载，所有操作受 generation、依赖、兼容性和反向依赖检查约束。

当前版本按独立 DSH 部署点承载租户边界，每点具有独立机器身份和本地版本状态；尚不能据此宣称同一 DSH 内多个用户具有独立的租户版本空间。本地操作权限沿用现有管理栅栏。

租户完成的业务结果是：

- A 点可以启用 v2，B 点继续使用 v1；中心发布新版本不会自动改变任一点；
- 检查更新只读元数据，网络失败、撤权或下架不会被显示为“已是最新”；
- 新版下载或启用失败时，旧版仍可运行；已缓存版本可在中心和 Git 断开时回退；
- 运行时只读本地、已验证的快照，不在任务执行过程中访问远程中心；切换版本不破坏已经捕获旧快照的长任务。

**当前状态：插件链路已接通，真实租户闭环未验收。** 宿主客户端、持久 operation、库存/状态事务、四页签和本地 runtime 快照已经实现；相关宿主回归 162/162、中心 176/176、协议/归档 85/85，编译浏览器流程 2/2 通过，插件全库 934/934 通过。已经启动过两套隔离真实 DSH 进程并验证端口、`DSH_HOME`、插件加载和本地接口隔离，但两套进程尚未完成真实中心绑定、v1/v2 下载、显式启用、断网运行和离线回退，因此 `A08/A10–A18/A22/A24` 仍不能标为通过。

## 3. 版本和身份规则

### 3.1 包身份

中心的 `packId` 是全局身份，不能只靠提交表单字段覆盖包内 `pack.json`。新包推荐使用组织命名空间形式 `orgslug.packslug`；带点号的 ID 必须使用所属组织前缀，安全的历史裸 ID 可在全局唯一、组织归属和清单一致的前提下继续使用。提交字段、包清单、发布清单和本地库存仍必须一致，这个约束保护组织隔离、实体冲突检查和不可覆盖发布。

因此，`macro-capital-analyst` 作为安全历史裸 ID 可以归属于组织 `macro`，前提是当前提交、包清单和发布清单一致且全局唯一；不需要在中心偷偷建立别名，也不能把本地 `2.2.0` 伪装成已审核 release。新包仍应优先采用 `orgslug.packslug`，以减少跨组织冲突。

### 3.2 三个版本概念

| 概念 | 所属 | 含义 |
|---|---|---|
| Git 源版本 | 开发者仓库 | 可变分支/tag 指向的输入，只有固定 commit 后才进入审核 |
| 中心发布版本 | 中心 | 已批准、签名、不可覆盖的 `releaseId + digest`，可授权下载 |
| 租户运行版本 | 部署点 | 本地已验证并由租户显式启用的 release；每个部署点独立 |

任何“中心有新版本”都不等于“租户已经更新”。租户的运行版本以本地状态和实际 runtime 快照为准。

## 4. 当前交付分层

| 层 | 当前结论 | 主要证据 | 仍缺什么 |
|---|---|---|---|
| 协议/校验/安全归档 | 组件完成 | `packages/pack-contract`、`packages/pack-artifact`；F02/F03 记录；协议/归档 85/85 | 与真实双实例贯通 |
| 中心后端与持久任务 | 第一阶段主链路通过 | `apps/pack-center`；`PACK-CENTER-PHASE1-EVIDENCE.md`；中心 70/70；真实 Git/OIDC/PG/Worker 浏览器流程 | 真实部署硬配额、恢复和第二实例复建 |
| 开发者网页 | 真实公开 Git 主流程通过 | `PACK-CENTER-PHASE1-EVIDENCE.md`；A01/A03 阶段证据 | 租户安装/更新/回退业务链 |
| 平台审核/发布治理 | 第一阶段审核、签名、授权下载通过 | `PACK-CENTER-PHASE1-EVIDENCE.md`；C04–C07 及真实 HTTP 证据 | 完整治理页面边缘路径、备份恢复 |
| 租户宿主客户端 | 组件与编译浏览器通过 | `PACK-CENTER-P4-HOST-CLIENT-EVIDENCE.md`、`PACK-CENTER-P5-HOST-UI-EVIDENCE.md` | 两真实 DSH 的绑定、下载、启用、离线回退 |
| DSH runtime 接入 | 已有本地快照桥接 | `src/host/pack-runtime.ts`、P5 真实进程探针 | 长任务跨切换和实际 v1/v2 运行证明 |
| 交付/生产部署 | 未完成 | 规划和隔离证据已形成 | S3/配额、备份恢复、第二实例按指南重建；生产试点后置 |

## 5. 接下来只做能闭环的顺序

1. 第一阶段真实公开 Git 校验、审核、签名发布和授权下载已完成；样例、源码指纹和逐项证据见 `PACK-CENTER-PHASE1-EVIDENCE.md`。
2. 用这个已发布 release 绑定两个隔离 DSH，分别安装同一包的 v1/v2；A 点启用新版本，B 点保持旧版本，记录 release、摘要、generation 和 runtime 快照。
3. 断开中心与 Git，验证两点仍能执行既有包、A 点回退并保持旧版；注入下载/启用失败，确认旧版不丢失。
4. 再补长任务快照、完整浏览器更新/回退、legacy 接管、中心重启/Worker 中断/备份恢复和第二实例复建。
5. 只有 A01–A24 都有对应运行证据，才能把整体 Goal 从“未完成”改成“完成”；第一阶段 Goal 已单独完成，不替代整体验收。

## 6. 非目标与边界

首版不包含私有 Git 凭据、ZIP 上传、在线 CLI 发布、匿名公共注册、自动升级、中心远程推送安装和可执行插件脚本。生产域名、生产身份、正式凭据、DSH 核心和现有生产服务不在本轮修改范围内。

详细接口、状态机和原始验收矩阵继续以 [PACK-CENTER-DESIGN.md](PACK-CENTER-DESIGN.md)、[PACK-CENTER-DEVELOPMENT-PLAN.md](PACK-CENTER-DEVELOPMENT-PLAN.md) 与 [PACK-CENTER-GOAL-ACCEPTANCE.md](PACK-CENTER-GOAL-ACCEPTANCE.md) 为准。
